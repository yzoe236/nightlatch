# ProfileLock 开发笔记(中文,给自己看的)

> 公开门面是英文 `README.md`;这份是自用备忘,记决策理由和踩过的坑。

## 一句话定位
给共用电脑(实验室公共机)上自己的 Chrome profile 加一道密码帘子。**核心卖点=锁定状态不跨设备同步**——竞品把解锁状态存 `storage.sync`,导致"在家解锁,实验室那台也开了",保护恰好在人不在场时失效。

## 三层存储(全项目最重要的设计,别改)
- `storage.sync` — 密码哈希、共享偏好(时长/主题/网站列表):跟账号走,一次设置全设备通用
- `storage.session` — **锁定状态**:本机内存、绝不同步、浏览器重启即清空 → 每台各锁各的,重启必回锁定
- `storage.local` — **本机开关**(闲置自动锁开/关)、错误尝试记录:每设备独立

改任何设置项之前先问:**它属于哪一层?** 放错层就会重蹈竞品覆辙。

## 关键实现决策
- **闲置语义**:用页面活动心跳(20s 节流)+ 窗口焦点/切标签 + alarms 每分钟检查,判定"这个 profile 多久没人动"。**不用 `chrome.idle` 的系统 idle**——共用电脑上别人一直在用,系统永不闲置,自己的 profile 就永不自动锁。系统 idle 只保留 `locked` 事件(Win+L 必锁)。
- **先遮后问 fail-closed**:content script 在 document_start 先建遮罩,再问后台锁没锁——避免锁定时页面内容闪现。
- **不封 chrome://extensions**:故意留的逃生门(忘密码可移除重装)。封了会把自己锁死,而且封不住真正想绕的人。
- **manifest `key` 字段**:钉死扩展 ID,让两台机器的 unpacked 安装被认作同一扩展,`storage.sync` 才互通(密码只设一次)。私钥 `dev-key.pem` 已 gitignore,**绝不能提交**;上架商店前要剥掉 key(`--store` 构建自动处理)。

## 变现决策(2026-07-31 定案:不做付费)
- 曾按竞品(Google Chrome Profile Lock,印度独立开发者,2000用户)的切法做过完整付费层:license key + Free/PRO 分层。**v1.0 决定全部拆掉,产品完全免费开源**。
- 理由:①这个品类的命门是**信任**——要用户把密码敲进陌生扩展,开源是唯一可信的答案;②那点授权费本来就是零花钱级(竞品体量说明问题);③license secret 必然在扩展包内,技术上守不住;④**H-1B 合规**:免费开源+无收入是最安全姿势,收钱涉及未授权工作风险(见 IDEAS #001/#004 闸门)。
- **仍然保留的商业想象力**:B2B 批量部署授权(实验室/图书馆/学校 IT 强制安装)。开源杀不掉它——机构买的是部署、支持和能签合同的对象,而且可审计代码在机构采购里是加分项。

## 待办
- [ ] 双机实测核心验收:实验室锁着 → 家里解锁 → 实验室必须仍锁着
- [ ] 图标(16/48/128)、商店截图(1280×800)、隐私政策页
- [ ] Chrome Web Store 上架($5 开发者账号,先 Unlisted 跑通流程)
- [ ] 上架申报 **non-trader**(免费发布不公示地址;EU trader 规则会公开经营者住址电话——竞品开发者的家庭住址就这么被挂出来的)

## 相关记录
- 想法台账:`OneDrive\Leo\ideas-奇思妙想\IDEAS.md` #004
- 上架与收费路线全文:`OneDrive\Leo\daily-万事屋\攻略_Chrome插件上架与收费路线_2026-07-30.md`
- 姊妹项目 OneFill(AI 填表插件):`D:\dev\onefill`,台账 #002
