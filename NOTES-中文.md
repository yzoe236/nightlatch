# Nightlatch 开发笔记(中文,给自己看的)

> 公开门面是英文 `README.md`;这份是自用备忘,记决策理由和踩过的坑。

## 一句话定位
给共用电脑(实验室公共机)上自己的 Chrome profile 加一道密码帘子。**核心卖点=锁定状态不跨设备同步**——竞品把解锁状态存 `storage.sync`,导致"在家解锁,实验室那台也开了",保护恰好在人不在场时失效。

## 三层存储(全项目最重要的设计,别改)
- `storage.sync` — 密码哈希、共享偏好(**默认**时长/主题/网站列表):跟账号走,新机器装上就继承这套
- `storage.session` — **锁定状态**:本机内存、绝不同步、浏览器重启即清空 → 每台各锁各的,重启必回锁定
- `storage.local` — **本机开关**(闲置自动锁开/关)、**本机时长**、错误尝试记录:每设备独立

改任何设置项之前先问:**它属于哪一层?** 放错层就会重蹈竞品覆辙。

### 时长优先级(v0.5.0 修的 bug)
`plk_device.autolockMin`(本机) > `plk_cfg.autolockMin`(同步) > `DEFAULT_AUTOLOCK_MIN`

Leo 2026-08-06 报的:在任意一台改时长,所有电脑跟着变。根因是 `effectiveAutolockMin()`
收了 `devicePrefs` 参数**却只从 `cfg` 取值**,per-device 层当时只装了一个布尔。

**同步层是故意留着的**,它是"新机器的默认值",别为了修这个 bug 把它删掉——删了新装的
机器就没有合理初值。设备层写 `null` 表示撤销覆盖、回去跟随默认值,`setLocalPrefs()`
会把值为 null 的键**删掉**而不是存一个 null 进去。

这条有测试守着:`node test/autolock.test.js`(15 项)。测试是**从 background.js 正文里
切出真函数**跑的,不是抄的副本,所以改了实现测试会跟着动。

### 恢复码(2026-08-15 加)
以前忘密码只能去 chrome://extensions 卸载重装,设置全丢。竞品里有两家做了找回,这是真实痛点。

- **格式**:Crockford base32(去掉 I/L/O/U),4 组 5 位共 20 字符 = **100 bit 熵**,远强于普通密码
- **只存哈希**,跟密码走同一条 PBKDF2 路径。明文只在 `PLK_SET_PASSWORD` / `PLK_RECOVER`
  的响应里返回一次,**丢了谁也变不出来**
- **归一化**很宽松:大小写、连字符、空格随便,I/L 认成 1、O 认成 0(照 Crockford 惯例)。
  用户是手抄到纸上再打回来的,不宽松就没人用得成
- ⚠️ **共用同一个冷却池**(`plk_fail`)。恢复码是密码等价凭据,单独开一扇没防护的门
  等于把整个暴力破解防护绕过去
- ⚠️ **改密码时轮换恢复码**。不轮换的话,"因为怀疑泄露而改密码"这个动作就是白做的
- `PLK_RECOVER` 有 `fromExtPage` 保护,注入的遮罩(content script)发不了,
  所以入口只能在 `locked.html`。设新密码这种事不该从任意网页发起
- `locked.js` 里的 `holdOpen` 标志:恢复成功后新码在屏幕上,**必须挡住自动跳走**,
  否则用户还没抄下来页面就没了

### 解锁死循环(0.6.1 修):输完密码 Chrome 整个退出,重开又要密码

Leo 原话:「很久了之后,输入密码之后它自己就退出了。我又重新点进 Google Chrome,
它又要我去输入密码。这感觉就像是一个 bug,最后就陷入了一个循环。」

⚠️ **这个 bug 差点被查错方向。** 「它自己就退出了」语音转写分不出是「Chrome 整个退出」
还是「刚解锁又被锁回去」。我先按后者查,找到两条竞态,**都是真的,但都不是他遇到的那个**。
问了 Leo 一句,他答「Chrome 整个退出了」,真正的成因才浮出来。
**教训:歧义就是要问,不要挑一个自己更喜欢的解释往下跑。**

#### 真正的成因:锁屏落地页关掉了浏览器唯一的标签页

链条是确定性的,一步都不靠运气:

1. 浏览器重启 → `storage.session` 空 → 按设计必然锁着
2. 冷启动通常只有一个新标签页。Chrome 报的 URL 是 `chrome://newtab/`
3. 这个 URL **命中 `GUARD_RE`**(里面就有 `new-tab-page|newtab`,拦它是为了挡住最近访问磁贴),
   strict 模式默认开,于是 `guardSweep()` 把它赶去 `locked.html?from=chrome://newtab/`
4. `locked.js` 的 `fromOk` 只认 `http/https/file`,**`chrome://` 一律不认**
5. 于是解锁成功之后走的是 `else` 分支:`setTimeout(window.close, 800)`
6. 关掉唯一的标签页 = 关掉窗口 = 关掉最后一个窗口 = **Chrome 退出**
7. 重开 → `storage.session` 又是空的 → 又锁着 → 又要密码 → **回到第 1 步**

**每一环都是默认配置下的默认路径**,所以线上 6 个用户里,只要有人是「开着 Chrome 就一个新标签页」
的用法,就会撞上。这不是边角情况。

修法:`locked.js` **一处 `window.close()` 都不留**,改成向后台发 `PLK_LEAVE`,由后台
`chrome.tabs.update()` 把标签页导航回原处。导航被 Chrome 拒绝时才退回去关标签页,
而且**只在还有别的标签页时才关**。`from` 来自 URL 查询串(谁都能改),所以后台
**不照着导航,只拿它跟 `GUARD_RE` 比对**,对不上就退到新标签页。

证据:`node test/lasttab.test.js`,21 项。正则是从 `background.js` 和 `locked.js` 正文里
抠出来跑的,不是抄的副本,实现改了测试会跟着动。

#### 顺带修掉的两条竞态(不是 Leo 报的那个,但都是真的)

**一、`chrome.idle` 的 `'locked'` 事件被无条件相信。**
事件描述的是发出那一刻屏幕的状态,而监听器可能晚得多才跑到:MV3 的 worker 闲置就被拆掉,
靠事件唤醒,中间隔了多久代码根本不知道。迟到的 `'locked'` 落在刚输完密码之后就是原地锁回去。

⚠️ **第一版修法是错的,独立审查(strict)当场打回,这条值得单记。**
错的修法是「不信事件,当场再问一次 `queryState()`,说 `active` 就放过」。
漏洞在于**走开锁屏迟到的那个事件,查出来也是 `active`**:
人走开 → 屏幕锁了 → 机器睡了 → 恢复后人先解开 Windows → 事件才姗姗来迟。
按那个修法直接放过,**profile 在最该锁的场景下反而不锁**。

正确的判据不是「屏幕现在锁没锁」,而是**「这中间有没有人证明过自己知道密码」**。
所以复核只在 `plk_unlockedAt` 距今不到 10 秒的窗口里跑,其余一切情况**无条件锁**。
要进这个窗口必须先输对密码,而输对密码本来就是正门,攻击面没变大。
`plk_unlockedAt` 和 `plk_lastActive` 必须是两个字段:后者随便浏览一下就刷新,
答不了「刚才有人输过密码吗」。
窗口里也不是直接放过,**还是要问一次**:解锁完顺手按 Win+L 走人是真实场景,那时查出来是
`locked`,照锁。查询出任何岔子(老 Chrome 没有 promise、API 报错、根本不回调)一律**按锁处理**。

**明说的权衡**:解锁后 10 秒内、屏幕此刻确实是 `active` 的 `locked` 事件会被放过。
这是唯一一处行为变化。

**二、闹钟的判断跨了好几个 `await`,决定和写入之间隔着一段真空。**
`isLocked()` → 读 `plk_lastActive` → `tabs.query({audible})` 全是异步,最后那个要遍历所有标签页,
慢到足够盖住一次完整的解锁。判断用几小时前的时钟,写入却落在解锁之后,
于是把刚验过的密码**静默抹掉**。修法是给 `lockNow()` 加 `confirm` 回调,
**在真正写入的那一刻、拿着串行锁**重新验一遍条件。
「立刻锁」的调用方(Win+L、Ctrl+Shift+L、弹窗按钮)不传 `confirm`,永远说了算。

顺带把 `unlockNow()` 的三个字段合成一次原子写。以前先写 `plk_unlocked` 再写 `plk_lastActive`,
中间那一瞬间的状态是「已解锁但看起来闲置了几小时」,闹钟落进去照样锁。

还有两条是审查在同一轮里挑出来的,不在原始范围内:
`noteLockEvent` 的读改写没串行,两个触发器同时来会丢记录,给它**单开一条链**
(不能和状态那条共用,否则写日志会等在等日志的锁上);
`PLK_SET_PASSWORD` 判完 `isLocked()` 还要过 `guardedVerify`,那一步可能排队很久,
排完不重判就 `unlockNow()`,等于给「只有 PLK_UNLOCK 和 PLK_RECOVER 能开锁着的 profile」
这句设计意图开了个后门(前提是知道当前密码,不算越权,但和注释写的不一致)。

#### 锁定原因记录

`plk_lockLog` 存 `storage.local`,封顶 20 条,只有时间戳和原因字符串,不同步、不含网址和凭据。
设置页新增「Why this profile locked」一栏。被忽略掉的事件也留痕(`screen-lock-stale`)。
**它顺便能分辨那句歧义**:Chrome 整个退出不会留下任何新记录,被锁回去会写清是锁屏还是闲置超时。

测试:`node test/relock.test.js` 31 项 + `node test/lasttab.test.js` 21 项。
两套都靠 `test/sw-harness.js`,把**真的 `background.js`** 塞进一套假 `chrome.*` 里跑,
存储是带延迟的异步,所以跨 `await` 的交错在这里会真的发生。
同一套测试指向 `git show 297596d:background.js`(线上 0.4.0)两条竞态都复现,
**不是新引入的回归,从第一版就在**。


### 每台电脑不同时长:功能早就有了,但从没上架过(2026-08-28 查实)
Leo 又报了一次「在一台电脑上改时长,所有电脑跟着变」。这和 2026-08-06 报的是**同一件事**,
当时修在 commit `36287e2`,发在 **0.5.0**。
问题是**商店到今天还挂着 0.4.0**(2026-08-03 更新,`fetch_web.py` 查证),0.5.0 和 0.6.0 都没传上去。
所以他三台机器装的都是没有 per-device 时长的那一版,代码修了等于没修。
**结论:这个不用再写代码,催上架就行。**

## 关键实现决策
- **闲置语义**:用页面活动心跳(20s 节流)+ 窗口焦点/切标签 + alarms 每分钟检查,判定"这个 profile 多久没人动"。**不用 `chrome.idle` 的系统 idle**——共用电脑上别人一直在用,系统永不闲置,自己的 profile 就永不自动锁。系统 idle 只保留 `locked` 事件(Win+L 必锁)。
- **先遮后问 fail-closed**:content script 在 document_start 先建遮罩,再问后台锁没锁——避免锁定时页面内容闪现。
- **不封 chrome://extensions**:故意留的逃生门(忘密码可移除重装)。封了会把自己锁死,而且封不住真正想绕的人。
- **manifest `key` 字段**:钉死扩展 ID,让两台机器的 unpacked 安装被认作同一扩展,`storage.sync` 才互通(密码只设一次)。私钥 `dev-key.pem` 已 gitignore,**绝不能提交**;上架商店前要剥掉 key(`--store` 构建自动处理)。

## 变现决策(2026-07-31 定案:不做付费)
- 曾按竞品(Google Chrome Profile Lock,印度独立开发者,2000用户)的切法做过完整付费层:license key + Free/PRO 分层。**v1.0 决定全部拆掉,产品完全免费开源**。
- 理由:①这个品类的命门是**信任**——要用户把密码敲进陌生扩展,开源是唯一可信的答案;②那点授权费本来就是零花钱级(竞品体量说明问题);③license secret 必然在扩展包内,技术上守不住;④**H-1B 合规**:免费开源+无收入是最安全姿势,收钱涉及未授权工作风险(见 IDEAS #001/#004 闸门)。
- **仍然保留的商业想象力**:B2B 批量部署授权(实验室/图书馆/学校 IT 强制安装)。开源杀不掉它——机构买的是部署、支持和能签合同的对象,而且可审计代码在机构采购里是加分项。

## 待办
- [ ] 双机实测核心验收:实验室锁着 → 家里解锁 → 实验室必须仍锁着
- [ ] 图标(16/48/128)、商店截图(1280×800)、隐私政策页
- [ ] Chrome Web Store 上架($5 开发者账号,先 Unlisted 跑通流程)
- [ ] 上架申报 **non-trader**(免费发布不公示地址;EU trader 规则会公开经营者住址电话——竞品开发者的家庭住址就这么被挂出来的)

## 相关记录
- 想法台账:`OneDrive\Leo\ideas-奇思妙想\IDEAS.md` #004
- 上架与收费路线全文:`OneDrive\Leo\daily-万事屋\攻略_Chrome插件上架与收费路线_2026-07-30.md`
- 姊妹项目 OneFill(AI 填表插件):`D:\dev\onefill`,台账 #002
